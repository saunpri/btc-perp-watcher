# Optional environment settings for Watcher
# Absolute path to the config.json used by the collector
WATCHER_CONFIG=/opt/watcher/config.json

# Optional base URL where you later publish output files
WATCHER_PUBLIC_BASE_URL=https://watcher.example.com/output
